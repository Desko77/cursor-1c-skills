{
  "name": "Ссылочный тип проверяется по индексу конфигурации (Check 16)",
  "setup": "empty-config",
  "preRun": [
    {
      "script": "meta-compile/scripts/meta-compile",
      "input": {
        "type": "Catalog",
        "name": "Контрагенты"
      },
      "args": {
        "-JsonPath": "{inputFile}",
        "-OutputDir": "{workDir}"
      }
    },
    {
      "script": "meta-compile/scripts/meta-compile",
      "input": {
        "type": "Catalog",
        "name": "Заявки",
        "attributes": [
          "Клиент: CatalogRef.Контрагенты"
        ]
      },
      "args": {
        "-JsonPath": "{inputFile}",
        "-OutputDir": "{workDir}"
      }
    },
    {
      "script": "config-index/scripts/config-index",
      "args": {
        "-ConfigPath": "{workDir}",
        "-OutFile": "{workDir}/index.json"
      }
    }
  ],
  "params": {
    "objectPath": "Catalogs/Заявки",
    "indexPath": "index.json"
  },
  "args_extra": [
    "-Detailed"
  ],
  "noSnapshot": "meta-validate только читает и печатает - эталон зафиксировал бы выход preRun (meta-compile и config-index), а не проверяемого навыка; проверяется stdout",
  "expect": {
    "stdoutContains": "16. Reference types: 1 checked against index, all resolve"
  }
}
