---
description: "Безопасная запись BSL-модулей через EDT MCP (режимы write_module_source)"
globs: ["**/*.bsl"]
alwaysApply: false
---

# Безопасная запись BSL через write_module_source

Самая опасная зона при работе с кодом 1С через EDT MCP: неверный режим записи может затереть весь модуль или потерять часть кода. Прочитать ДО первой записи в модуль.

Применяется: при записи BSL через `write_module_source` (ai-edt). Дополняет `file-edit-efficiency.md` (выбор инструмента) и `mcp-tool-priority.md` (маршрут и обязательные проверки).

---

## Режимы write_module_source и их риск

| Режим | Что делает | Риск |
|-------|-----------|------|
| `searchReplace` | Находит фрагмент и заменяет. Затрагивает только найденное | Низкий - режим по умолчанию для точечной правки |
| `replaceMethod` | Заменяет один метод целиком по имени | Низкий - безопасен для правки метода |
| `replaceLines` | Заменяет диапазон строк (с защитой `expectedText`) | Средний - см. ниже |
| `insertBefore` / `insertAfter` | Вставка относительно якоря | Низкий |
| `append` | Добавляет в конец модуля | Низкий |
| `replace` | **Перезаписывает ВЕСЬ файл модуля** | Высокий - см. правило 1 |

После записи EDT автоматически валидирует результат (синтаксис) и возвращает ошибки в ответе - проверять их сразу.

---

## Правило 1: НИКОГДА не использовать replace для одного метода

`replace` заменяет весь файл модуля. Передав один метод - удалишь весь остальной код (сотни/тысячи строк), без отката.

Для правки одного метода - `replaceMethod` (по имени) или `searchReplace` (старый текст метода в поиск, новый в замену). `replace` оправдан только когда: модуль создается впервые, либо модуль пустой/менее ~50 строк и передается его полное содержимое, либо полная перезапись модуля по явному согласованию с пользователем.

## Правило 2: oldSource (searchReplace) = доказательство, что прочитал текущий код

Для `searchReplace` искомый фрагмент должен встречаться в файле РОВНО один раз. Ноль совпадений - операция отклонена; больше одного - тоже отклонена с подсказкой взять более крупный уникальный фрагмент. Это защищает от замены не того места и подтверждает, что ты видел актуальное содержимое. При ошибке "not found / multiple" - НЕ ретраить вслепую: перечитать метод (`read_method_source`) и обновить/расширить искомый фрагмент до уникального.

## Правило 3: replaceLines требует expectedText

`replaceLines` сверяет фактический текст заменяемых строк с переданным `expectedText`. Если код в файле сдвинулся (номера строк устарели) - запись отклоняется с diff, файл не меняется. Всегда передавать `expectedText` и не доверять номерам строк из старого чтения.

---

## Алгоритм безопасной правки метода

1. `read_method_source` - прочитать метод, запомнить текущее состояние (и номера строк).
2. При необходимости оценить размер модуля - `get_module_structure`.
3. Сформировать `write_module_source`: `mode=replaceMethod` (или `searchReplace` с уникальным `oldSource`), новый полный текст метода (с директивами `&НаКлиенте`/`&НаСервере`/`&НаСервереБезКонтекста`).
4. Проверить ответ на синтаксические ошибки (EDT валидирует автоматически).
5. `revalidate_objects` по затронутому объекту, затем `get_project_errors` с фильтром по нему - именно в таком
   порядке: без перевалидации маркеры остаются от прошлого состояния.
6. `ask_1c_ai` (1c-naparnik) - обязательная проверка качества; замечания проверять по факту до внесения
   правок (`mcp-tool-priority.md`, "Обязательные проверки").

## Алгоритм отката неудачной записи

Встроенного undo у `write_module_source` нет. Откат держится на том, что ты помнишь предыдущее содержимое ДО записи.

1. **Перед каждой `searchReplace` держать в контексте и `oldSource` (что заменяешь), и новый текст** - не выбрасывать, пока не подтвердил корректность записи.
2. Если в ответе видно, что заменен не тот фрагмент или сломалась структура - **немедленно** выполнить обратную `searchReplace`: передать новый текст как `oldSource`, а прежний `oldSource` как замену. Чем быстрее, тем меньше шанс, что открытый редактор EDT увидит сломанный файл.
3. Если обратная замена уже невозможна (новый текст не уникален в файле, или модуль прошел через `replace`) - **остановиться и спросить пользователя**. НЕ реконструировать модуль "по памяти": в EDT может быть открыта копия в редакторе, восстановление через UI надежнее.
4. Для `replace` правило то же: держать полный старый текст до отправки нового, иначе откат невозможен.

---

## Чего нельзя делать

- Править `.bsl` напрямую через `Edit`/`Write`/`Read+Write` - только через `write_module_source`, иначе EDT не увидит изменений до refresh, и теряется авто-проверка и подсчет строк (прямая правка `.bsl` для BSL отключена - `mcp-tool-priority.md`, обязательные проверки).
- `replace` без полного содержимого нового модуля.
- Передавать усеченное содержимое в надежде "сервер разберется" - запишется ровно то, что передано.
- `skipSyntaxCheck=true` чтобы "обойти" ошибку баланса блоков - это почти всегда симптом реального бага в коде.
- Коммитить, если `get_project_errors` показывает новые проблемы в измененном модуле.

---

## Связанные правила

- `file-edit-efficiency.md` - выбор инструмента чтения/записи, экономия токенов.
- `mcp-tool-priority.md` - обязательный `ask_1c_ai` после записи BSL.
- `bsl-ssl.md` - при правке типовых модулей дополнительно разметка маркерами модификации.
